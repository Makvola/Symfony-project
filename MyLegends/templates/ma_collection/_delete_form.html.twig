<form method="post" action="{{ path('app_macollection_delete', {'id': macollection.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ macollection.id) }}">
    <button class="btn btn-primary">{{ button_label|default('Delete') }}</button>

</form>
